#!/bin/bash

# Change to the directory where the repository is located
cd /new_chatapp

# Pull the latest code from the git repository (make sure it's a git repo)
git pull origin main  # Replace 'main' with the branch you want to pull from
