version: 0.0
os: linux

files:
  - source: /
    destination: /new_chatapp

hooks:
  AfterInstall:
    - location: scripts/install_dependencies.sh
      timeout: 300
  ApplicationStart:
    - location: scripts/start_service.sh
      timeout: 300